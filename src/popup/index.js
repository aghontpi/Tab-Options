import React from 'react';
import ReactDOM from 'react-dom/client';
import PopupPage from '../pages/popup/popup.page.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PopupPage />
  </React.StrictMode>
);
